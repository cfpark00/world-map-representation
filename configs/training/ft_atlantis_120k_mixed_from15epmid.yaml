# Experiment directory - all outputs go here
exp_dir: "outputs/experiments/ft_atlantis_120k_mixed_from15epmid"

# Dataset
dataset:
  path: "/n/home12/cfpark00/WM_1/outputs/datasets/dist_120k_mixed"
  max_sequence_length: 32

# Tokenizer
tokenizer_path: "outputs/tokenizer/wm1_tokenizer"

# Model (Qwen2.5-like)
model:
  vocab_size: 44  # Our custom tokenizer vocab size
  hidden_size: 128
  num_hidden_layers: 6
  num_attention_heads: 4
  intermediate_size: 512
  init_scale: 0.1  # Standard deviation for weight initialization (GPT-2 style)
  ckpt: /n/home12/cfpark00/WM_1/outputs/experiments/dist_1M_no_atlantis_15epochs/checkpoints/checkpoint-70320

# Training
training:
  batch_size: 128  # Larger batch size since we have 1M samples
  eval_batch_size: 64
  num_epochs: 5  # Fewer epochs since more data
  optimizer: "adamw"
  learning_rate: 5e-5
  weight_decay: 0.01
  scheduler: "linear_with_warmup"
  warmup_steps: 50  # More warmup for larger dataset
  seed: 42

# Checkpointing
checkpointing:
  save_strategy: "steps"
  save_steps: 0.05  # Save 10 times per epoch
  eval_strategy: "steps"
  eval_steps: 0.025  # Eval 40 times per epoch