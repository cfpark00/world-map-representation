output_dir: "data/cka_matrices"
input_dir: "scratch/cka_analysis_clean"  # This should point to where the CKA data is

layers: [3, 4, 5, 6]

# Plot settings
figure_dpi: 150
colormap: "magma"

# Which prefixes to analyze
prefixes: ["pt1", "pt2", "pt3"]