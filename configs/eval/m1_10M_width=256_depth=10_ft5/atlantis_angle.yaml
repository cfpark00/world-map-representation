# Evaluation configuration for distance_1M experiment

output_dir: "/n/home12/cfpark00/WM_1/data/experiments/m1_10M_width=256_depth=10_ft5/evals/atlantis_angle"

# Experiment to evaluate
experiment_dir: "/n/home12/cfpark00/WM_1/data/experiments/m1_10M_width=256_depth=10_ft5"

# Dataset path - should match what was used for training
dataset_path: "/n/home12/cfpark00/WM_1/data/datasets/angle_100k_atlantis_required"

# Evaluation settings
eval_batch_size: 512

# Generation parameters
max_generation_length: 32
temperature: 0.0
top_k: 1
do_sample: false

# Checkpoint selection
checkpoints: "all"  # Options: "all", "last", or list like [0, 1000, 5000]

# Compute
device: "cuda"

# Random seed
seed: 42

# Save full details
save_full_results: true  # Save all rows with text, prompt, expected, generation, metric